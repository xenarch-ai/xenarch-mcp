{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "io.github.xenarch-ai/xenarch-mcp",
  "description": "MCP server for AI agent payments. Check pricing, pay for x402-gated content and APIs with USDC on Base, and view payment history — non-custodial, no API keys required.",
  "repository": {
    "url": "https://github.com/xenarch-ai/xenarch-mcp",
    "source": "github"
  },
  "version": "0.1.3",
  "packages": [
    {
      "registryType": "npm",
      "identifier": "@xenarch/agent-mcp",
      "version": "0.1.3",
      "transport": {
        "type": "stdio"
      },
      "environmentVariables": [
        {
          "description": "Wallet private key (hex, 0x-prefixed). Auto-generates on first run if omitted.",
          "isRequired": false,
          "format": "string",
          "isSecret": true,
          "name": "XENARCH_PRIVATE_KEY"
        },
        {
          "description": "Base RPC endpoint URL",
          "isRequired": false,
          "format": "string",
          "isSecret": false,
          "name": "XENARCH_RPC_URL"
        },
        {
          "description": "Network for payments (base or base-sepolia)",
          "isRequired": false,
          "format": "string",
          "isSecret": false,
          "name": "XENARCH_NETWORK"
        },
        {
          "description": "Max USD to auto-approve without prompting",
          "isRequired": false,
          "format": "string",
          "isSecret": false,
          "name": "XENARCH_AUTO_APPROVE_MAX"
        }
      ]
    },
    {
      "registryType": "npm",
      "identifier": "@xenarch/agent-mcp",
      "version": "0.1.3",
      "transport": {
        "type": "streamablehttp"
      },
      "environmentVariables": [
        {
          "description": "Wallet private key (hex, 0x-prefixed). Auto-generates on first run if omitted.",
          "isRequired": false,
          "format": "string",
          "isSecret": true,
          "name": "XENARCH_PRIVATE_KEY"
        },
        {
          "description": "Base RPC endpoint URL",
          "isRequired": false,
          "format": "string",
          "isSecret": false,
          "name": "XENARCH_RPC_URL"
        },
        {
          "description": "Network for payments (base or base-sepolia)",
          "isRequired": false,
          "format": "string",
          "isSecret": false,
          "name": "XENARCH_NETWORK"
        },
        {
          "description": "Max USD to auto-approve without prompting",
          "isRequired": false,
          "format": "string",
          "isSecret": false,
          "name": "XENARCH_AUTO_APPROVE_MAX"
        },
        {
          "description": "Port for HTTP server (default: 3000)",
          "isRequired": false,
          "format": "string",
          "isSecret": false,
          "name": "PORT"
        },
        {
          "description": "Set to 'http' to enable HTTP transport",
          "isRequired": true,
          "format": "string",
          "isSecret": false,
          "name": "TRANSPORT"
        }
      ]
    }
  ]
}
